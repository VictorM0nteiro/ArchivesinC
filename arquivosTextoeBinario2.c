#include <stdio.h>
#include <stdlib.h>

int main(){
    // os dados são gravados exatamento como seriam
    // impressos na tela
    // Os dados gravados como caracteres de
    // 8 bits utilizando a tebela ascii
    // para isso existe uma etapa de conversão
    // dos dados
}